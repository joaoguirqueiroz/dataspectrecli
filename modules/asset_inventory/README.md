# Asset Inventory Module

Normaliza entradas de inventário autorizadas em uma estrutura padronizada para relatórios e histórico.

## Parâmetros

- `assets`: lista JSON ou string separada por vírgulas.
- `input_file`: arquivo JSON contendo uma lista ou `{ "assets": [...] }`.
- `source`: origem opcional dos dados.

